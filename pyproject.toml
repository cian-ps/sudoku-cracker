[project]
name = "sudoku-cracker"
version = "1.0.0-beta.1"
description = "Solve any Sudoku puzzle in seconds."
readme = "README.md"
requires-python = "==3.12.3"
dependencies = [
    "kivy>=2.3.1",
    "numpy==2.4.6",
    "onnxruntime>=1.17.0",
    "opencv-python>=4.13.0.92",
    "pip>=26.1.2",
    "plyer>=2.1.0",
    "rapidocr>=3.9.0",
]

[project.optional-dependencies]
paddleocr = [
    "paddleocr==3.3.3",
    "paddlepaddle==3.2.0",
]
dev = [
    "mypy>=2.1.0",
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
    "ruff>=0.15.16",
    "buildozer>=1.6.0",
    "cython==0.29.34",
    "setuptools>=82.0.1",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.9",
]

[tool.mypy]
ignore_missing_imports = true

[tool.pytest.ini_options]
markers = [
    "no_mock_ocr: disable the autouse OCR engine mock for a test",
    "integration: requires real OCR models and runs full inference",
]

[tool.ruff]
exclude = [
    "*.ipynb"
]
